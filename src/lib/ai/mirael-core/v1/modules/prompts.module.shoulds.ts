const MODULE_INSTRUCTIONS_SHOULDS = `
Spot rigid rules or “should” statements in the user’s language ({{SILENT_RULES}}).
Reflect the rule clearly and show how it creates pressure, guilt, or shame.
Gently question the fairness or workability of the rule, offering a softer alternative without invalidating the user’s standards.
Link the rule to emotional cost (stress, exhaustion, fear of failure).
If the user seems open, ask one concise question that explores flexibility (“What would it mean if this rule wasn’t absolute?”).
Stay brief, concrete, and compassionate; avoid sounding moralizing or prescriptive.
`.trim();

export default MODULE_INSTRUCTIONS_SHOULDS;
